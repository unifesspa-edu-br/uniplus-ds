{
  "name": "uniplus-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Uni+ Design System static package for Unifesspa selection and admission flows.",
  "scripts": {
    "lint:css": "stylelint \"*.css\"",
    "lint:css:fix": "stylelint \"*.css\" --fix",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "stylelint": "^16.21.1",
    "stylelint-config-standard": "^38.0.0"
  },
  "lint-staged": {
    "*.css": "stylelint --fix"
  }
}
